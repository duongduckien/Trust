import { StyleSheet } from 'react-native';

export const styleSheet = StyleSheet.create({
    images: { 
        width: 50, 
        height: 50,
    },
});

export const styles = {
    activeOpacity: 0.7,
}